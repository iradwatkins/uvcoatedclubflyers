# Blue-Green Upstream Configuration
# Switch between blue and green by commenting/uncommenting

# Blue environment (active by default)
upstream uvcoated_app {
    server uvcoated-app-blue:3000;
    keepalive 32;
}

# Green environment (uncomment to activate, comment blue above)
# upstream uvcoated_app {
#     server uvcoated-app-green:3000;
#     keepalive 32;
# }
